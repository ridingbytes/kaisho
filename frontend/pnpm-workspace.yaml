# esbuild ships a prebuilt binary and needs its install
# script to put it in place; vitest will not start without
# it. Approved explicitly because pnpm blocks build scripts
# by default, which is the right default.
allowBuilds:
  esbuild: true
